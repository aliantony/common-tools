package ${package.Mapper};

import java.util.List;
import com.antiy.common.base.IBaseDao;
import ${package.Entity}.${entity};
import ${package.Entity}.vo.query.${entity}Query;
import ${package.Entity}.vo.response.${entity}Response;

/**
 * <p>
 * $!{table.comment} Mapper 接口
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${table.mapperName} extends IBaseDao<${entity}> {

    List<${entity}Response> findList${entity}(${entity}Query query) throws Exception;
}
