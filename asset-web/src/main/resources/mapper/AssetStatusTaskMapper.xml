<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.antiy.safety.dao.AssetStatusTaskDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.antiy.safety.entity.AssetStatusTask">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="asset_id" property="assetId"/>
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_id, asset_id
    </sql>

    <sql id="Table_Name">
        asset_status_task
    </sql>


    <sql id="IF_Base_Column_List">
        <if test=" taskId != null">
            task_id,
        </if>
        <if test=" assetId != null">
            asset_id,
        </if>
    </sql>

    <sql id="IF_Value_Column_List">
        <if test=" taskId != null">
            #{taskId },
        </if>
        <if test=" assetId != null">
            #{assetId },
        </if>
    </sql>

    <sql id="Set_Column_List">
        <if test=" taskId != null">
            task_id = #{taskId },
        </if>
        <if test=" assetId != null">
            asset_id = #{assetId },
        </if>
    </sql>

    <sql id="Where_Column_List">
        <if test=" taskId != null">
            and task_id = #{taskId }
        </if>
        <if test=" assetId != null">
            and asset_id = #{assetId }
        </if>
    </sql>

    <insert id="insert" parameterType="com.antiy.safety.entity.AssetStatusTask" useGeneratedKeys="true" keyProperty="id"
            keyColumn="id">
        insert into
        <include refid="Table_Name"></include>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="IF_Base_Column_List"></include>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <include refid="IF_Value_Column_List"></include>
        </trim>
    </insert>

    <update id="update" parameterType="com.antiy.safety.entity.AssetStatusTask">
        update
        <include refid="Table_Name"></include>
        <trim prefix="SET" suffixOverrides=",">
            <include refid="Set_Column_List"></include>
        </trim>
        where id = #{id}
    </update>

    <!-- 根据主键查询 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="Table_Name"></include>
        where id = #{id}
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        delete from
        <include refid="Table_Name"></include>
        where id = #{id}
    </delete>

    <select id="getByWhere" resultMap="BaseResultMap" parameterType="java.util.Map">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
    </select>

    <select id="findCount" resultType="int" parameterType="com.antiy.common.base.ObjectQuery">
        select count(1)
        from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
    </select>

    <select id="findQuery" resultMap="BaseResultMap" parameterType="com.antiy.common.base.ObjectQuery">
        select * from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
        <if test="pageSize !=-1">
            LIMIT #{pageSize} offset #{pageOffset}
        </if>
    </select>
</mapper>