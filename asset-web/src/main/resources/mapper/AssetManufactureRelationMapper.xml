<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.antiy.asset.mapper.AssetManufactureRelationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.antiy.asset.entity.AssetManufactureRelation">
        <id column="id" property="id"/>
        <result column="unique_id" property="uniqueId"/>
        <result column="business_id" property="businessId"/>
        <result column="asset_id_hardsoft_lib" property="assetIdHardsoftLib"/>
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, unique_id, business_id, asset_id_hardsoft_lib
    </sql>

    <sql id="Table_Name">
        asset_manufacture_relation
    </sql>


    <sql id="IF_Base_Column_List">
        <if test=" uniqueId != null">
            unique_id,
        </if>
        <if test=" businessId != null">
            business_id,
        </if>
        <if test=" assetIdHardsoftLib != null">
            asset_id_hardsoft_lib,
        </if>
    </sql>

    <sql id="IF_Value_Column_List">
        <if test=" uniqueId != null">
            #{uniqueId },
        </if>
        <if test=" businessId != null">
            #{businessId },
        </if>
        <if test=" assetIdHardsoftLib != null">
            #{assetIdHardsoftLib },
        </if>
    </sql>

    <sql id="Set_Column_List">
        <if test=" uniqueId != null">
            unique_id = #{uniqueId },
        </if>
        <if test=" businessId != null">
            business_id = #{businessId },
        </if>
        <if test=" assetIdHardsoftLib != null">
            asset_id_hardsoft_lib = #{assetIdHardsoftLib },
        </if>
    </sql>

    <sql id="Where_Column_List">
        <if test=" uniqueId != null">
            and unique_id = #{uniqueId }
        </if>
        <if test=" businessId != null">
            and business_id = #{businessId }
        </if>
        <if test=" assetIdHardsoftLib != null">
            and asset_id_hardsoft_lib = #{assetIdHardsoftLib }
        </if>
    </sql>

    <insert id="insert" parameterType="com.antiy.asset.entity.AssetManufactureRelation" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        insert into
        <include refid="Table_Name"></include>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="IF_Base_Column_List"></include>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <include refid="IF_Value_Column_List"></include>
        </trim>
    </insert>

    <update id="update" parameterType="com.antiy.asset.entity.AssetManufactureRelation">
        update
        <include refid="Table_Name"></include>
        <trim prefix="SET" suffixOverrides=",">
            <include refid="Set_Column_List"></include>
        </trim>
        where id = #{id}
    </update>

    <!-- 根据主键查询 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="Table_Name"></include>
        where id = #{id}
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        delete from
        <include refid="Table_Name"></include>
        where id = #{id}
    </delete>

    <select id="getByWhere" resultMap="BaseResultMap" parameterType="java.util.Map">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
    </select>

    <select id="findCount" resultType="int" parameterType="com.antiy.common.base.ObjectQuery">
        select count(1)
        from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
    </select>

    <select id="findQuery" resultMap="BaseResultMap" parameterType="com.antiy.common.base.ObjectQuery">
        select * from
        <include refid="Table_Name"></include>
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
        </trim>
        <if test="pageSize !=-1">
            LIMIT #{pageSize} offset #{pageOffset}
        </if>
    </select>
</mapper>